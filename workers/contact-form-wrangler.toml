name = "nepal-jyotish-contact-form"
main = "contact-form-worker.ts"
compatibility_date = "2024-11-12"

# Custom domain route for contact form API
[[routes]]
pattern = "nepaljyotish.org/api/contact"
zone_name = "nepaljyotish.org"

# Environment variables will be set via GitHub Actions
# GOOGLE_SHEETS_WEBHOOK_URL will be set as a secret in GitHub